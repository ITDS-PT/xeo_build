XEO Build System Instructions

0) Checkout this project, clone the XEO and XWC Projects to your local hard drive
1) If the concatenator.jar does not exist, run ant -f build_concatenator.xml to create it

2) Set the version and path to git repositories in ant/global.properties 
	(xeo.buildnumber, path to xeo git repository and xwc git repository properties respectively, set the branches you want to build - You can build xeo with a specific branch and xwc with a different branch)
3) Run ant -f build.xml to create necessary structures
4) Run ant -f BuildProject.xml to generate the XAR file
 
 
 


NOTES : Currently it does not create tags in the repository nor does it copy the generated to XAR to anywhere